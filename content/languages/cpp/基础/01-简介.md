---
title: "简介"
weight: 1
---

## 编译器

- GNU C++
- LLVM Clang++
- Visual Studio C++

## 系统

- Windows
- MacOS
- Linux

## 集成开发环境（IDE）

- Visual Studio （Windows）
- CLion （全平台）
- XCode （MacOS）

## 编辑器

- vim
- vscode
- 各种 AI 编辑器（在 vscode 上修改的）

## hello world

```cpp
#include <iostream>  //引入头文件

int main() { // C++程序入口（main 函数）， int 为返回类型
    std::cout << "Hello World!" << std::endl;  //标准输出，语句，以分号结尾。
    return 0; //返回值
}
```

## 注释

```cpp
// 单行注释
/*
  多行注释
*/
```

## 转义字符

| 转义字符 | 说明 |
| :-: | :-: |
| `\n` | 换行符 |
| `\t` | 制表符|
| `\r` | 回车符|
| `\a` | 警告字符|
| `\\` | 反斜杠字符|
| `\'` | 单引号字符|
| `\"` | 双引号字符|

## 运算

整型类型、加号运算符

```cpp
#include <iostream>

int main() {
   int number1{0}; // 列表初始化为 0，不初始化可能是个随机值， int 类型为整型变量
   int number2{0};
   int sum{0}; //

   std::cout << "Enter first integer: ";
   std::cin >> number1; // 输入值 std：：cin ，变了必须提前定义才能使用

   std::cout << "Enter second integer: ";
   std::cin >> number2;

   sum = number1 + number2; //右侧结果赋值给左侧变量。

   std::cout << "Sum is " << sum << "\n";// \n 换行
} 
```

### C++算术运算符对应表

| 操作       | 算术运算符 | C++表达式 |
|------------|------------|-----------|
| 加法       | +          | f + 7     |
| 减法       | -          | p - c     |
| 乘法       | *          | b * m     |
| 除法       | /          | x / y     |
| 取余（求模）| %          | r % s     |

### 相等性与关系运算符表

| 代数运算符 | C++ 运算符 | 示例条件 | 含义 |
|------------|------------|----------|------|
| **关系运算符** | - | - | - |
| > | > | x > y | x 大于 y |
| < | < | x < y | x 小于 y |
| ≥ | >= | x >= y | x 大于等于 y |
| ≤ | <= | x <= y | x 小于等于 y |
| **相等性运算符** | - | - | - |
| = | == | x == y | x 等于 y |
| ≠ | != | x != y | x 不等于 y |

运算符简单使用

```cpp
#include <iostream> // 引入头文件

using std::cout; // using 整个文件使用std namespace下的 cout，不用每次都写std：：
using std::cin;

int main() {
   int number1{0};
   int number2{0};
   
   cout << "Enter two integers to compare: ";
   cin >> number1 >> number2;

   if (number1 == number2) { // if 判断， == 相等
      cout << number1 << " == " << number2 << "\n";
   }

   if (number1 != number2) { // != 不等
      cout << number1 << " != " << number2 << "\n";
   }

   if (number1 < number2) { // < 小于
      cout << number1 << " < " << number2 << "\n";
   }

   if (number1 > number2) { // > 大于
      cout << number1 << " > " << number2 << "\n";
   }

   if (number1 <= number2) { // <= 小于等于
      cout << number1 << " <= " << number2 << "\n";
   }

   if (number1 >= number2) { // >= 大于等于
      cout << number1 << " >= " << number2 << "\n";
   }
}
```

